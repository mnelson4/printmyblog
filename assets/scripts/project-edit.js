// alert('ye');
jQuery(document).ready(function(){
	var sections = document.getElementById('pmb-project-sections');
	var sortable = Sortable.create(sections);
	var choices = document.getElementById('pmb-choices');
	var sortable = Sortable.create(choices);
});
